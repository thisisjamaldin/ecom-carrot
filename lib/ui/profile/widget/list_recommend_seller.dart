import 'package:flutter/material.dart';

class ListRecommendSeller extends StatelessWidget {
  const ListRecommendSeller({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('sdflkmsdflkmsdlksmd');
  }
}
